export interface User {
    id: string;
    name: string;
    email: string;
    createdAt: Date;
}

export interface Version {
    vername: string;
    changes: string;
    date: string;
    changeType: string;
}
